# 專案架構及程式碼分析

這份文件詳細分析了 **E-seal 電子章** 應用程式的專案架構、程式碼邏輯及運作流程。

## 1. 專案概述

**E-seal 電子章** 是一個基於 **Electron** 框架開發的桌面應用程式，旨在提供 PDF 文件電子用印功能。使用者可以載入 PDF 文件，從本地模型庫中選擇印章（如發行章、公司章），將其拖曳至指定位置，並支援日期顯示與調整，最後匯出蓋好章的 PDF 文件。

### 技術堆疊
- **核心框架**: Electron
- **前端邏輯**: Vanilla JavaScript (ES6+)
- **樣式**: Vanilla CSS (CSS3 Variables, Flexbox/Grid)
- **PDF 處理**:
  - `pdfjs-dist`: 用於在前端渲染 PDF 預覽。
  - `pdf-lib`: 用於在後端合併圖片與 PDF，生成最終檔案。
- **互動操作**: `interactjs` (處理拖曳與放開)。

---

## 2. 目錄結構

```text
E-seal電子章/
├── node_modules/       # 專案依賴庫
├── dist/               # 打包輸出目錄 (electron-packager)
├── seal_model/         # 存放印章圖片資源 (png/jpg)
├── main.js             # [主進程] Electron 入口點，管理視窗與系統 IO
├── preload.js          # [預加載] 連接 Main 與 Renderer 的安全橋樑
├── renderer.js         # [渲染進程] 前端 UI 邏輯、PDF 渲染、印章操作
├── index.html          # 應用程式主介面
├── style.css           # 應用程式樣式表
├── package.json        # 專案設定與依賴定義
└── ... (其他 PDF 測試檔與說明文件)
```

---

## 3. 核心模組分析

### 3.1 主進程 (`main.js`)

**職責**: 負責應用程式的生命週期管理、建立視窗，以及處理需要高權限的系統操作（如檔案讀寫）。

- **`createWindow()`**: 初始化 1200x900 的主視窗，並載入 `preload.js` 以啟用 contextIsolation。
- **IPC Handlers (處理渲染進程的請求)**:
  - `select-pdf`: 呼叫系統對話框選擇 PDF，讀取檔案並轉為 Base64 回傳。
  - `list-seals`: 掃描 `seal_model`目錄，回傳所有可用印章圖片的 Base64 與名稱。
  - `choose-save-path`: 呼叫系統存檔對話框，讓使用者選擇匯出路徑。
  - `write-pdf`: 接收最終合成的 Base64 資料並寫入磁碟。

### 3.2 預加載腳本 (`preload.js`)

**職責**: 透過 `contextBridge` 將特定的與主進程通訊的能力暴露給渲染進程 (`window.api`)，確保安全性。

- **Exposed APIs**:
  - `api.selectPdf()`
  - `api.listSeals()`
  - `api.chooseSavePath(suggestedName)`
  - `api.writePdf(payload)`

### 3.3 渲染進程 (`renderer.js`)

**職責**: 這是應用程式最核心的邏輯部分，包含 UI 互動、PDF 頁面渲染、印章生成與拖曳邏輯。

#### 關鍵變數
- `pdfDoc`: 載入的 PDF 文件物件 (pdfjs-dist)。
- `placements`: 儲存所有已放置印章的狀態陣列 (座標、頁碼、縮放、日期設定等)。
- `seals`: 從主進程獲取的可用印章列表。

#### 主要功能模組

1.  **初始化 (`loadSeals`)**:
    - 啟動時透過 `window.api.listSeals()` 載入印章圖片並渲染左側列表。
    - 每個印章卡片包含預覽圖與選取框 (Checkbox)。

2.  **PDF 載入與預覽 (`loadPdf`, `renderPage`)**:
    - 使用 `pdfjsLib.getDocument` 解析 PDF。
    - `renderPage` 利用 Canvas 繪製當前頁面。
    - 建立 `overlay-layer` (覆蓋層)，用於在 Canvas 之上放置可互動的印章 DOM 元素。

3.  **印章操作 (`addStampInstance`, `createStampElement`)**:
    - 使用者勾選印章後，點擊圖片可將印章加入當前頁面。
    - **位置管理**: 每個印章實例 (`stamp-instance`) 都是一個絕對定位的 DOM 元素。
    - **日期合成**:
        - 若開啟日期顯示，會動態生成包含當日日期的 `div` 或 Canvas 圖像疊加在印章上。
        - 支援針對「發行章」與普通章採用不同的預設參數 (如 `dateScale`, `font-size`)。
    - **互動 (`interactjs`)**: 賦予印章 `.draggable` 能力，並限制在父容器內移動。
    - **調整功能**: 點擊印章後，可透過右側面板調整大小 (`Size`)、日期大小 (`Date Scale`) 與垂直偏移 (`Y Offset`)。

4.  **匯出邏輯 (`exportBtn.click`, `buildStampedPdf`)**:
    - 使用 `pdf-lib` 載入原始 PDF。
    - 遍歷 `placements` 陣列，將對應的印章圖片繪製到目標 PDF 頁面的正確座標上 (需進行 UI 座標到 PDF 座標的轉換)。
    - **日期繪製**: 使用 Canvas (`createTextImage`) 動態生成日期文字的 PNG 圖片，再嵌入 PDF 指定位置。這是為了確保匯出的 PDF 上日期樣式與預覽一致。
    - 最終生成 Base64 並透過 `api.writePdf` 存檔。

### 3.4 樣式設計 (`style.css`)

- **設計風格**: 
  - 採用深色模式 (`Dark Mode`)，主色調為深藍 (`#0f172a`) 搭配強調色天空藍 (`#38bdf8`)。
  - 使用 `backdrop-filter: blur` 製作毛玻璃效果的 Header。
  - 介面佈局使用 CSS Grid (左側工具欄 + 右側預覽區)。

---

## 4. 關鍵流程圖解 (文字版)

### 流程一：使用者開啟並操作
1. **啟動 APP**: `main.js` 建立視窗 -> `index.html` 載入 -> `renderer.js` 執行 `loadSeals()` 顯示左側印章。
2. **上傳 PDF**: 點擊「上傳 PDF」 -> `main.js` 讀檔回傳 -> `renderer.js` 用 `pdfjs-dist` 渲染第一頁。
3. **加入印章**: 
   - 使用者勾選想要的印章 (如：發行章)。
   - 點擊印章圖片 -> `addStampInstance` 在 `placements` 新增資料 -> 在 `overlay-layer` 建立可拖曳 DOM。
4. **調整位置**: 使用者拖曳印章 -> `interact.js` 更新 DOM 位置與 `placements` 內的 `x, y` 座標。
5. **輸出**: 點擊「輸出 PDF」 -> `buildStampedPdf` 將所有 DOM 狀態「燒錄」進 PDF 檔案 -> `main.js` 存檔。

### 流程二：座標轉換機制
在預覽時，PDF 頁面通常會經過縮放 (Scale) 以適應螢幕。
- **預覽**: `ViewWidth = PDF真實寬度 * Scale`
- **匯出**: 寫入 PDF 時，需將 UI 上的座標還原：
  ```javascript
  PDF_X = UI_X / Scale
  PDF_Y = PDF_Height - (UI_Y + UI_Height) / Scale  // PDF 座標原點在左下，UI 在左上
  ```

---

## 5. 待優化或注意事項
1. **日期字型**: 目前使用系統預設 `sans-serif`，若不同電腦無對應字型，匯出結果可能略有差異。
2. **記憶體管理**: 若 PDF 頁數極多，`pdfDoc` 與 Canvas 緩存可能會佔用較多記憶體。
3. **錯誤處理**: 目前對於 PDF 解析失敗或寫入權限不足僅有簡單 `alert` 提示。

---
*文件生成時間: 2026/01/29*
